services:
  app:
    build: .

    container_name: acquisitions-api

    env_file:
      - .env.development

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    command: npm run dev

    restart: unless-stopped